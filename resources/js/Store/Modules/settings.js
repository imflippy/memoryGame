

const state = {
  windowWidth: window.screen.width
};

const getters = {
  getWidth: state => state.windowWidth
};

const mutations = {

};

const actions = {

};

export default {
  state,
  getters,
  mutations,
  actions
}
